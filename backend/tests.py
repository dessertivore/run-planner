# from resources import add_run, find_run_func, add_plan, find_plan_func,
from resources import set_run_reminder


# add_plan(1, "10k training plan")
# add_plan(2, "Half marathon in 10 weeks")
# assert find_plan_func(2)[0][0] == "Half marathon in 10 weeks"
# assert find_plan_func("Half marathon in 10 weeks")[0][0] == 2


# add_run(1, 1, 1, "5km, easy")
# assert find_run_func(1, 1, 1)[0][0] == "5km, easy"

set_run_reminder(0)
